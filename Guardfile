guard 'jekyll-plus', :serve => true, :drafts => true, :future => true, :extensions => ['js', 'css', 'erb', 'svg', 'png', 'jpg'] do
  watch %r{^source/}
  watch %r{_config.yml}
end

guard 'livereload' do
  watch %r{^gh-pages/}
end
